<%# TODO: Do we really need both panel and card? panel is for bootstrap but I like the card language %>
<fieldset class="<%= dom_class(object, name) %> panel panel-default">
  <legend class="panel-heading">
    <%# TODO: Translate the name into a question %>
    <h3 class="panel-title"><%= name %></h3>
  </legend>
  <div class="panel-body">
    <%= yield %>
  </div>
</fieldset>
