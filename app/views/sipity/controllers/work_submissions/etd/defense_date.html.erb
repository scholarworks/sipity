<h1>Defense Date</h1>

<%= simple_form_for(
  model,
  url: request.path,
  method: :post,
  as: :work,
  html: { class: 'simple-form panel panel-primary with-footer' }
) do |f| %>
  <legend class="panel-heading">
    <h3 class="panel-title">Please select the <b>date</b> of your defense</h3>
  </legend>

  <fieldset class="panel-body">
    <p class="panel-hint">This date <em>should</em> be in the past (or you’re getting ahead of yourself).</p>
    <%= f.error_notification %>
    <%= f.error :base, error_method: :to_sentence %>

    <%= f.input :defense_date, as: :date, input_html: { class: 'form-control' }, wrapper_html: { class: 'form-inline' }, include_blank: true %>
  </fieldset>

  <div class="panel-footer action-pane">
    <%= f.submit nil, name: "form/#{model.processing_action_name}/submit" %>
  </div>
<% end %>

<div class="action-pane">
  <%= link_to t("sipity/decorators/entitiy_enrichments.cancel"), work_path(model.work), class: 'btn btn-default' %>
</div>
