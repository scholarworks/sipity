<h1><%= model.translate(:call_to_action, predicate: :heading) %></h1>

<div class="panel-tiles">
  <%= simple_form_for(model, url: request.path, method: :post, as: :work, html: { class: 'simple-form panel panel-primary with-footer' }) do |f| %>
    <legend class="panel-heading">
      <h3 class="panel-title"><%= model.translate(:panel, predicate: :title) %></h3>
    </legend>

    <fieldset class="panel-body">
      <%= f.error_notification %>
      <%= f.error :base, error_method: :to_sentence %>
      <%= f.input :submitted_for_publication, as: :boolean, inline_label: 'Submitted for publication?', label: false, wrapper_class: 'checkbox' %>
      <%= f.input :submission_accepted_for_publication, as: :select, collection: f.object.possible_submission_accepted_for_publication, input_html: { class: 'form-control' } %>
      <%= f.input :publication_name, as: :multi_value, input_html: { class: 'form-control' } %>
    </fieldset>

    <div class="panel-footer action-pane">
      <%= f.submit nil, name: "form/#{model.processing_action_name}/submit" %>
    </div>
  <% end %>
</div>

<div class="action-pane">
  <%= link_to t("sipity/decorators/entitiy_enrichments.cancel"), work_path(model.work), class: 'btn btn-default' %>
</div>
